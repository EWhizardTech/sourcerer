[project]
name = "sourcerer-gateway"
version = "0.1.0"
description = "API gateway: reverse proxy with SSE passthrough and aggregate health."
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
    "httpx>=0.28.0",
]

[tool.uv]
package = false
